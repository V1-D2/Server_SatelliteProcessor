# Core dependencies
numpy>=1.21.0
matplotlib>=3.4.0
h5py>=3.0.0
pyproj>=3.0.0
Pillow>=8.0.0
scipy>=1.7.0
xarray>=0.19.0

# Progress bars
tqdm>=4.60.0

# PyTorch for ML models (CPU/GPU support)
torch>=2.0.0
torchvision>=0.15.0

# OpenCV for image processing
opencv-python-headless>=4.5.0

# For GPORTAL API
requests>=2.25.0
# gportal  # Install manually if not available via pip

# Additional ML dependencies
timm>=0.9.0  # For SwinIR models

# Server-specific optimizations
numba>=0.53.0  # For numerical acceleration
joblib>=1.0.0  # For parallel processing

# Optional but recommended
psutil>=5.8.0  # For system monitoring
pyyaml>=5.4.0  # For configuration files